# Synergie-ITCI Shared Workflows

This repository hosts organisation-controlled GitHub Actions reusable workflows.

Current workflow:

- `.github/workflows/github-slack-router.yml`

Production callers must reference a release tag such as `@v1`.
